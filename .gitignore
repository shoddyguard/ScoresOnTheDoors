# This file is created by a tool, manual changes will be lost unless added to the designated section below.

## Auto generated ignores: ##
# Paket is used to restore various dependencies (including the Brownserve.PSTools PowerShell module).`nWe deliberately ignore the 'paket.lock' file as our projects are usually not precious about taking the latest versions of packages.

paket.lock
packages/
paket-files/

# The temporary directory is used to store build output, log files, redirected output etc. It is recreated by the _init.ps1 script.
.tmp/

# AI coding tool directories contain local/personal configuration and session data that should not be committed.
.claude/
.cursor/
.aider*
.continue/

# Environment files often contain secrets and should never be committed.
.env
.env.*

# Docker build output and cache.
.docker/

## Manually defined ignores: ##

# Node / JavaScript dependencies
node_modules/
/.pnp
.pnp.js
.yarn/install-state.gz

# Next.js build output
/.next/
/out/
/build
next-env.d.ts

# TypeScript incremental build cache
*.tsbuildinfo

# Local SQLite database (the dev DB is disposable)
*.db
*.db-shm
*.db-wal

# Keep the committed env template (the .env.* rule above would otherwise hide it)
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
